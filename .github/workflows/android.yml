name: CarteNabil-Android-Build-APK-Final

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.3'
          channel: 'stable'

      - name: Create Android Native Folders
        run: |
          flutter create --org com.supportclub --project-name carte_nabil . --platforms=android --no-overwrite

      - name: Inject Google Services JSON
        run: |
          if [ -n "${{ secrets.GOOGLE_SERVICES_JSON_BASE64 }}" ]; then
            echo "${{ secrets.GOOGLE_SERVICES_JSON_BASE64 }}" | base64 --decode > android/app/google-services.json
          else
            echo "Warning: GOOGLE_SERVICES_JSON_BASE64 secret not found."
          fi

      - name: Set Release Permissions & App Name
        run: |
          # Correction du Package Name
          sed -i 's/package=".*"/package="com.supportclub.carte"/' android/app/src/main/AndroidManifest.xml
          # Nettoyage des anciennes permissions pour éviter les doublons avant injection
          sed -i '/<uses-permission/d' android/app/src/main/AndroidManifest.xml
          # Injection propre des permissions
          sed -i '/<manifest/a \    <uses-permission android:name="android.permission.INTERNET" />\n    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />\n    <uses-permission android:name="android.permission.CAMERA" />\n    <uses-permission android:name="android.permission.RECORD_AUDIO" />\n    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />\n    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />\n    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />\n    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />' android/app/src/main/AndroidManifest.xml
          # Application label
          sed -i 's/android:label=".*"/android:label="ASSIMA-10"/' android/app/src/main/AndroidManifest.xml

      - name: Patch Android App Build Config
        run: |
          # 1. Forcer Namespace + ApplicationId (Regex tolerante aux espaces et =)
          sed -i -E 's/namespace[[:space:]]*=?[[:space:]]*".*"/namespace = "com.supportclub.carte"/g' android/app/build.gradle
          sed -i -E 's/applicationId[[:space:]]*=?[[:space:]]*".*"/applicationId = "com.supportclub.carte"/g' android/app/build.gradle

          # 2. Forcer minSdk à 23
          sed -i -E 's/minSdk[A-Za-z]*[[:space:]]*=?[[:space:]]*.*/minSdk = 23/g' android/app/build.gradle

          # 3. Forcer targetSdk à 34
          sed -i -E 's/targetSdk[A-Za-z]*[[:space:]]*=?[[:space:]]*.*/targetSdk = 34/g' android/app/build.gradle

          # 4. Ajouter les plugins Google Services
          if ! grep -q "com.google.gms:google-services" android/build.gradle 2>/dev/null; then
            sed -i '/dependencies {/a \        classpath '\''com.google.gms:google-services:4.4.1'\''' android/build.gradle 2>/dev/null || true
          fi
          if ! grep -q "com.google.gms.google-services" android/app/build.gradle; then
            echo "apply plugin: 'com.google.gms.google-services'" >> android/app/build.gradle
          fi

          # 5. Nettoyage agressif du Manifest
          sed -i '/android:minSdkVersion="[0-9]*"/d' android/app/src/main/AndroidManifest.xml

          # Vérification
          echo "--- VERIFICATION BUILD.GRADLE ---"
          grep -E "namespace|applicationId|minSdk|targetSdk" android/app/build.gradle

      - name: Build APK Release
        run: |
          flutter pub get
          flutter build apk --release --no-shrink --target-platform android-arm64 --split-per-abi

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: ASSIMA-10-Final-APK
          path: build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
